import Colors from '../../styles/colors';

const styles = () => ({
    letterPreview: {
        border: `1px solid ${Colors.grey}`,
        padding: 20,
        maxHeight: 275,
        overflow: 'scroll',
    },
});

export default styles;
