import Colors from './colors';

const styles = () => ({
    errorMessage: {
        paddingTop: 5,
        fontSize: 15,
        fontWeight: 400,
        textAlign: 'left',
        color: Colors.error,
    },
});
export default styles;
